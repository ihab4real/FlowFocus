import React from "react";
import TaskBoard from "../index";

/**
 * This is the main container for the TaskBoard feature.
 * It serves as the entry point for the TaskBoard functionality.
 */
export function TaskBoardContainer() {
  return <TaskBoard />;
}

export default TaskBoardContainer;
